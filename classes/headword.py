import re

class Headword():
    """
    The representation of the headword/lemma. This should be inputed by the user
    as the dictionary form of the word. EG va, vais, allez et al. should be placed
    under the headword aller, which this does in contain a word 
    """
